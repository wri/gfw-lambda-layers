# Packaging metadata
*-info/*
*/tests/*
python/pip/*
python/pkg_resources/*
python/setuptools/*
python/wheel/*

# numpy pure-Python source and type stubs are not needed at runtime —
# all real functionality is in the compiled .so extensions
python/numpy/**/*.py
python/numpy/**/*.pyi
